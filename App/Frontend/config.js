// ---------------------------------------------------------------------------
// EDIT THIS: the public hostname of your Cloudflare Tunnel, no trailing slash.
// This is the one value that ties the GitHub Pages frontend to your Pi.
//
// It must match a hostname in your cloudflared ingress config, and the same
// origin this page is served from (https://spicerke.github.io) must be listed
// in the API's ALLOWED_ORIGINS env var.
// ---------------------------------------------------------------------------
window.CLAIM_API_BASE = "https://claims.example.com";
